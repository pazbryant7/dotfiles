#!/usr/bin/env sh

RECORDING_ICON="^c#FFFFFF^[REC]^d^"
PID_FILE="/tmp/screen_recorder.pid"

is_recording() {
  [ -f "$PID_FILE" ]
}

format_recording() {
  printf "%s | " "$RECORDING_ICON"
}

main() {
  is_recording && format_recording
}

main "$@"
