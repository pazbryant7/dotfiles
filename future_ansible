cloning notes repository
cloning nvim repository
cloning dotfiles repository

installing all apps manually 

sh -c "$(wget https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh -O -)"

installing paru

install rust
curl https://sh.rustup.rs -sSf | sh

install paru manually (download zip and make)

install manually zsh plugins with oh-my-zsh plugin manager
	- p10k
	- zsh autosuggestiosn
	- zsh highlighst colors
	- you should use


git permissions to bandwitch manually

create ssh keys manually ( because the last one got lost )

creata manually screenshots video folder in video folder

create manually a symlink to my walppapers folder in mega to my pictures folder (wall)

create manually a symlink a Cristiano video motivational video to my video folder

create manually a symlink from my custom keyboard to xorg default keyboard (/etc/x11/xorg.conf)

manually installing pacman, pipx ,flatpack, cargo applications

config manually the fuck

adding manually product sans and quick sans font to /usr/config/fonts

manually adding all my symlinks

manually add nvim configuration to nvchad

create manually .zfunc folder in home directory and add script
bob complete zsh > ~/.zfunc/_bob

manually add nvm bin and completions

create external folder by hand ( for my backup with rsync )

add my crontab config script manually (user and admin crontab)


config arch keyboard by hand
config arch locale by hand

changing fonts manually .confg/fonts/config / lxapareance

manually install firefox theme
bash -c "$(curl -fsSL https://raw.githubusercontent.com/black7375/Firefox-UI-Fix/master/install.sh)"

adding manually keyboard /etc/x11/xorg.conf.d/00-keyboard.conf

I have to download and isntall fonts in a web page 
https://www.opendesktop.org/p/1598493

manually installation of pipewire
https://wiki.archlinux.org/title/PipeWire

manually install grub theme and move in
/boot/grub/themes/

I have to installa nnn from source and st terminal from source also

install manually tmux plugin manager tmp

install manually fonts font folder in firefox

adda custom theme
https://www.gnome-look.org/p/1661959

create completion script
fnm completions --shell <SHELL>

need to enable manually
systemctl --user enable +
pipewire.service
pipewire-pulse.service
wireplumber.service

enable manually mpd user service
systemctl --user enable mdp.service

manually create a postgress user
sudo su postgres
CREATE USER yourname WITH SUPERUSER PASSWORD 'yourpassword';
I have to create a new role, in this case the name of my 
linux user, bryant, after that I had to give it permissions
to login with 'WITH' option and SUPERUSER privilegiee

manually install cargo-update

manually installing lazydocker with go
go install github.com/jesseduffield/lazydocker@latest

create docker image manually and run it manually (tachiyomi)

install harpoon to zellij
rustup target add wasm32-wasi

install manually gd from pacman to source st terminal

configure keybinds vsconsole
at /etc/vconsole.conf

add this to  /etc/sudoer
Defaults    env_keep += "VISUAL EDITOR"

create a crontab config
0 */3 * * * /home/bryant/bin/sh/sync-password-manager.sh

I need to copy my custom keyboard layouts to
/usr/share/X11/xkb/symbols/

and then execute those commands
localectl set-x11-keymap  bryant-dvorak (xorg graphic keyboard)
localectl localectl set-keymap --no-convert dvorak (vconsole)

manually install zsh plugins
git clone https://github.com/zsh-users/zsh-autosuggestions ~/.zsh/zsh-autosuggestions

git clone https://github.com/zsh-users/zsh-syntax-highlighting.git ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting

copy manually this file and adding to my zsh plugin confi
https://github.com/ohmyzsh/ohmyzsh/blob/master/plugins/sudo/sudo.plugin.zsh

install manually tachidesk image with docker
